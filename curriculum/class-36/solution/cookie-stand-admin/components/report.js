import React from 'react'

export default function Report({ report }) {
    return (
        <div>
            <p>Report Table Coming Soon...</p>
            <pre><code>{JSON.stringify(report)}</code></pre>
        </div>
    )
}
